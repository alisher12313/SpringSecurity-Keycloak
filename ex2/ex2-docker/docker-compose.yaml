version: "3"

services:
  postgresql:
    image: postgres:latest
    container_name: ex2-postgres
    environment:
      POSTGRES_DB: testdb
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
    ports:
      - "5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data: